<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
      <template id="runbot.git_config">
[core]
	repositoryformatversion = 0
	filemode = true
	bare = true
<t t-foreach="repo_group.repo_ids" t-as="repo_id">
[remote "<t t-esc="repo_id.origin"/>"]
	url = repo_id.name
  <t t-if = "repo_id.fetch_heads">
	fetch = +refs/heads/*:refs/<t t-esc='repo_id.remote_name'/>/heads/*
  </t>
  <t t-if = "repo_id.fetch_pull">
	fetch = +refs/pull/*/head:refs/<t t-esc='repo_id.remote_name'/>/pull/*
  </t>
</t>
      </template>
    </data>
</odoo>
